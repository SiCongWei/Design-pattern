工厂方法，可以通过工厂接口的不同的具体实现类，来实现不同的产品。
比如本例中，producer是一个接口，里面有一个produce方法，返回值为sender类型。
那么现在有两种sender的实现类，sms和email。
所以通过实现producer，返回值的具体类型分别为sender和email就可以对两种不同产品进行操作。
当需要新加一个sender类型的实现类，只需要实现producer接口即可，而不需要更改工厂类中的代码。